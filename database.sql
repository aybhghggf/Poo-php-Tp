create database oop_1;

use oop_1 ;

create table users(
    id int primary key auto_increment,
    Nom varchar(255),
    Prenom varchar(255),
    Email varchar(255),
    Telephone varchar(255),
    Pass varchar(255)
);